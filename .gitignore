node_modules/
dist/
dist-web/
# Só o .env.example é versionado; qualquer outro .env* fica de fora
# (.env.local, .env.production… não estavam cobertos).
.env
.env.*
!.env.example
.env-builder
.env-skills-langwizard
*.log
.DS_Store
coverage/
tasks/
sync.sh
run-rebuild.sh
run-builder.sh
run-local.sh
run-prod.sh
run-mcp-inspector.sh
docker-compose-builder.yml

# Material de referência local (a página do LangWizard usada no redesign)
tmp/

# Servidores de smoke test do Claude Code (portas e banco descartáveis locais)
.claude/launch.json
